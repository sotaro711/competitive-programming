#include<stdio.h>
#define MAX_N 100009
#define MAX_M 200009

int N, M;
int G[MAX_N][MAX_M];
int deg[MAX_N];

// forward star : 隣接リストを配列で実装したもの
// 3つの配列を使用 : head[v], nxt[e], to[e]

// 各頂点の隣接要素を、固定サイズの配列で持つ方式



// long long : 10^9以上
// それ以下はint でおk
// 10^9 以下と保証されるため
int main() {
    scanf("%d %d", &N, &M);
    for (int i = 1; i <= M; i++) {
        int a, b;
        scanf("%d %d", &a, &b);
        G[a][]
    }


}
