#include<stdio.h>

int main() {
    int D, N, L[100009], R[100009];
    // scanfではアドレス
    scanf("%d", &D);
    scanf("%d", &N);
    for (int i = 1; i <= N; i++) {
        scanf("%d %d", &L[i], &R[i]);
    }

    
}