#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;

    // 1, 10, 100円の硬貨と1000円の紙幣

    vector<int> A(N+1);

    int hun, ten, one;
    for (int i = 1; i <= N; i++) {
        cin >> A[i];

        // A[i]より大きい最小の紙幣
        
    }

    // お釣りでもらった硬貨の枚数を出力

    return 0;
}
