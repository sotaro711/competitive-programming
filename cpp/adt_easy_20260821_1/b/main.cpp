#include <bits/stdc++.h>
using namespace std;

int N, M, X, T, D;
int main() {
    cin >> N >> M >> X >> T >> D;   

    if (N >= X) {
        cout << T << endl;
        return 0;
    } 

    int Sabun = 

    return 0;
}
